import { EditorView } from '@emirror/pm/view';
import React from 'react';

type Props = {
  view: EditorView;
  plugins: Record<string, any>;
};

const Menu = (props: Props) => <div>test</div>;

export default Menu;
