import { Node as PMNode } from '@emirror/pm/model';

type Props = {
  /**
   * get the textContent of PMNode
   */
  languageExtractor?: (node: PMNode) => string;
};

export default function (props: Props) {}
